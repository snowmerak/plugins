[package]
name = "ringbuf-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
memmap2 = "0.9.4"
tokio = { version = "1.35.1", features = ["net", "io-util", "time", "sync", "rt", "macros"] }

[target.'cfg(windows)'.dependencies]
uds_windows = "1.1.0"
